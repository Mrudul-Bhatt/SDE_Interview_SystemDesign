# Search Autocomplete — Architecture Diagrams

---

## 1. High-Level System Architecture

```mermaid
flowchart TB
    subgraph Client["Client (Browser / Mobile)"]
        UI["Search Box"]
        Debounce["Debounce\n(100ms delay)"]
        ClientCache["Client Session Cache\n(prefix → results)"]
        UI -->|"keypress"| Debounce
        Debounce -->|"check local first"| ClientCache
    end

    subgraph Edge["Edge Layer"]
        CDN["CDN\n(static assets)"]
        LB["Load Balancer"]
    end

    subgraph API["Autocomplete API Servers\n(stateless × 3)"]
        Handler["Request Handler\n/autocomplete?q=appl"]
    end

    subgraph Cache["Cache Layer"]
        Redis["Redis\n(prefix → top-K JSON)\nTTL: 10 min\n~1M entries, ~500 MB"]
    end

    subgraph Trie_Servers["Trie Servers (sharded)"]
        TS_AF["Trie Shard A–F"]
        TS_GM["Trie Shard G–M"]
        TS_NZ["Trie Shard N–Z"]
    end

    subgraph Data_Pipeline["Data Collection Pipeline (async)"]
        Kafka["Kafka\n(search events)"]
        Spark["Spark Aggregator\n(hourly batch)"]
        TrieBuilder["Trie Builder\n(builds new trie)"]
        Kafka --> Spark --> TrieBuilder
        TrieBuilder -->|"hot-swap"| TS_AF & TS_GM & TS_NZ
    end

    ClientCache -->|"cache miss"| LB
    LB --> Handler
    Handler --> Redis
    Redis -->|"cache miss ~5%"| TS_AF & TS_GM & TS_NZ
    Handler -->|"log search event"| Kafka
```

---

## 2. Trie Node Structure (Pre-computed Top-K)

```mermaid
flowchart TB
    subgraph Trie["Trie for prefix 'ap' with K=3"]
        ROOT["root\ntopK: [apple×10M, app×8M, april×3M]"]

        A["'a'\ntopK: [apple×10M, app×8M, april×3M]"]
        P["'p'\ntopK: [apple×10M, app×8M, april×3M]"]

        subgraph after_ap["Subtree under 'ap'"]
            P2["'p'\ntopK: [apple×10M, app×8M, application×2M]\n← isEndOfWord (app)"]
            R["'r'\ntopK: [april×3M, april fools×1M]"]
        end

        subgraph after_app["Subtree under 'app'"]
            L["'l'\ntopK: [apple×10M, application×2M, applet×500K]"]
            END_APP["∎ END: app\nfreq=8M"]
        end

        ROOT --> A --> P
        P --> P2 & R
        P2 --> L & END_APP
    end

    style END_APP fill:#e8f5e9
    style ROOT fill:#fff3e0
```

```
Query: Search("ap")
  Walk: root → ['a'] → ['p']
  Return ['p'].topK = [apple×10M, app×8M, april×3M]
  Cost: O(2) = O(prefix_length) — no subtree traversal needed!

Without pre-computed topK:
  Walk: root → ['a'] → ['p']
  DFS entire subtree to collect all words
  Sort → take top K
  Cost: O(prefix_length + subtree_size × log subtree_size) — slow at scale
```

---

## 3. Typeahead Request Flow (User Types "app")

```mermaid
sequenceDiagram
    participant U as User
    participant B as Browser
    participant CC as Client Cache
    participant LB as Load Balancer
    participant API as API Server
    participant Redis as Redis Cache
    participant Trie as Trie Server

    U->>B: types 'a'
    B->>B: debounce timer starts (100ms)

    U->>B: types 'p' (within 100ms)
    B->>B: reset debounce timer

    U->>B: types 'p' (within 100ms)
    B->>B: reset debounce timer

    Note over B: 100ms passes — send request

    B->>CC: check cache for "app"
    CC-->>B: MISS (first time)

    B->>+LB: GET /autocomplete?q=app
    LB->>+API: route request

    API->>+Redis: GET "app"
    alt Cache HIT (95% of traffic)
        Redis-->>API: [apple, app store, application, ...]
        API-->>LB: 200 OK — results
        LB-->>B: results
        B->>CC: store "app" → results
        B-->>-U: display 5 suggestions (< 10ms)
    else Cache MISS (5% of traffic)
        Redis-->>-API: null
        API->>+Trie: lookup("app")
        Trie-->>-API: topK from trie node
        API->>Redis: SET "app" results EX 600
        API-->>LB: 200 OK — results
        LB-->>-B: results
        B->>CC: store "app" → results
        B-->>U: display 5 suggestions (< 100ms)
    end

    Note over B,CC: User backspaces to "ap"
    B->>CC: check cache for "ap"
    CC-->>B: HIT (was cached this session) — instant!
    B-->>U: display suggestions instantly (0ms)
```

---

## 4. Data Collection and Trie Update Pipeline

```mermaid
flowchart LR
    subgraph Sources["Search Events"]
        US["US Region\nSearch API"]
        EU["EU Region\nSearch API"]
        AS["Asia Region\nSearch API"]
    end

    subgraph Streaming["Real-time Stream"]
        Kafka["Kafka\n3 brokers\n(search_events topic)"]
        RealTime["Real-time Aggregator\n(Flink)\nTop-100 trending terms\npatched every 5 min"]
    end

    subgraph Batch["Hourly Batch (full rebuild)"]
        S3["S3\n(raw log archive)"]
        Spark["Spark Job\nGROUP BY term\nCOUNT last 7 days"]
        Aggregated["Aggregated\n(term, freq) pairs\n~1B rows"]
        Builder["Trie Builder\nbuild in memory\n~30 min"]
    end

    subgraph Serving["Serving Layer"]
        NewTrie["New Trie\n(offline built)"]
        OldTrie["Active Trie\n(serving traffic)"]
        Swap{"Atomic\nHot-Swap"}
        Cache["Redis Cache\n(flush on swap)"]
    end

    US & EU & AS --> Kafka
    Kafka --> RealTime
    Kafka --> S3
    S3 --> Spark --> Aggregated --> Builder --> NewTrie
    RealTime -->|"patch top-100"| OldTrie
    NewTrie --> Swap
    Swap -->|"swap pointer"| OldTrie
    Swap -->|"flush"| Cache
```

---

## 5. Distributed Trie Sharding

```mermaid
flowchart TB
    subgraph Router["Trie Router (ZooKeeper)"]
        ZK["ZooKeeper\nShard Routing Table\na-f → Shard 1\ng-m → Shard 2\nn-s → Shard 3\nt-z → Shard 4"]
    end

    subgraph Shards["Trie Shards (each with 3 replicas)"]
        subgraph S1["Shard 1: a–f"]
            S1P["Primary"]
            S1R1["Replica 1"]
            S1R2["Replica 2"]
            S1P -.->|"async"| S1R1 & S1R2
        end
        subgraph S2["Shard 2: g–m"]
            S2P["Primary"]
            S2R1["Replica 1"]
            S2R2["Replica 2"]
            S2P -.->|"async"| S2R1 & S2R2
        end
        subgraph S3["Shard 3: n–s"]
            S3P["Primary"]
        end
        subgraph S4["Shard 4: t–z"]
            S4P["Primary"]
        end
    end

    API["API Server"] -->|"query 'ap'"| ZK
    ZK -->|"→ Shard 1"| S1R1
    S1R1 -->|"top-K results"| API

    note["Note: reads go to replicas\nwrites (trie rebuild) go to primaries only\nSharding by first char is uneven (e > x)\nProduction: shard by hash(prefix[0:2])"]
    style note fill:#fff9c4
```

---

## 6. Ranking Score Calculation

```
For each completion candidate:

  score = base_frequency
        × recency_multiplier      (1.0 for today, 0.5 for 7 days ago)
        × personalization_boost   (2.0 if user searched this before)
        × trending_boost          (1.5 if search rate doubled last hour)

Example: user types "py"
  Candidate          base_freq   recency   personal  trending   score
  ─────────────────────────────────────────────────────────────────────
  python             5,000,000   1.0       1.0       1.0        5,000,000
  python tutorial    3,000,000   1.0       2.0       1.0        6,000,000  ← user searched before
  python download    2,000,000   1.0       1.0       1.5        3,000,000  ← trending
  pypi               1,500,000   1.0       1.0       1.0        1,500,000
  pytorch            1,000,000   1.0       1.0       1.0        1,000,000

  Ranking: python tutorial > python > python download > pypi > pytorch
```

---

## 7. Cache Decision Tree

```
GET /autocomplete?q={prefix}
    │
    ▼
Client session cache?
    ├── HIT  → return instantly (0ms, no network)
    └── MISS
            │
            ▼
        Redis.GET(prefix)
            ├── HIT  → return results (< 1ms)
            └── MISS
                    │
                    ▼
                Route to correct Trie shard
                    │
                    ▼
                Walk trie O(prefix_length)
                    │
                    ▼
                Return node.topK
                    │
                    ├──→ Redis.SET(prefix, results, EX 600)
                    └──→ return to client (< 5ms trie, < 10ms total)
```
